namespace BlueHarbor.Faro;

/// <summary>
/// Dimensione di una nave (e, di riflesso, della banchina che può ospitarla).
/// Insieme chiuso definito dalla commessa: XL, L, M, S.
/// </summary>
public enum ShipSize
{
    S,
    M,
    L,
    XL
}
